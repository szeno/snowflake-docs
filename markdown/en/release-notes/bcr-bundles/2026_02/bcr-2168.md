# TABLES view and SHOW TABLES command: New columns in output

Attention

This behavior change is in the 2026\_02 bundle.

For the current status of the bundle, refer to [Bundle history](/release-notes/bcr-bundles/2026_02_bundle#label-behavior-change-bundle-2026-02-status).

When this behavior change bundle is enabled, the output of the [TABLES](/sql-reference/info-schema/tables) view,
in the [INFORMATION SCHEMA](/sql-reference/info-schema), includes the following new column:

| Column name | Data type | Description |
| --- | --- | --- |
| ROW\_TIMESTAMP\_ON | TEXT | Describes whether the table includes a row timestamp.   - `YES` if the table includes row timestamp - `NO` otherwise |

Expand

Show lessSee more

When this behavior change bundle is enabled, the output of the [SHOW TABLES](/sql-reference/sql/show-tables) command includes the following new column:

| Column name | Data type | Description |
| --- | --- | --- |
| ROW\_TIMESTAMP | TEXT | Describes whether the table includes a row timestamp.   - `ON` if the table includes row timestamp - `OFF` otherwise |

Expand

Show lessSee more

Ref: 2168
