# SHOW VERSIONS IN MODEL command: `module_name` column in output renamed `model_name`

Attention

This behavior change is in the 2024\_07 bundle.

For the current status of the bundle, refer to [Bundle History](/release-notes/bcr-bundles/2024_07_bundle#label-behavior-change-bundle-2024-07-status).

When this behavior change bundle is enabled, the `module_name` column in the output of the [SHOW VERSIONS IN MODEL](/sql-reference/sql/show-versions-in-model)
command is renamed `model_name`. The content of the column (the name of the model) does not change.

Ref: 1700
