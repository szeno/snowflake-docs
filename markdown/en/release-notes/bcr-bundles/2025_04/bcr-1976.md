# Deprecation of the `SNOWFLAKE` user

Attention

This behavior change is in the 2025\_04 bundle.

For the current status of the bundle, refer to [Bundle History](/release-notes/bcr-bundles/2025_04_bundle#label-behavior-change-bundle-2025-04-status).

The deprecation of the `SNOWFLAKE` user behaves as follows:

Before the change:
:   A system-defined user called `SNOWFLAKE` exists in every account. This `SNOWFLAKE` user has the SNOWFLAKE\_SUPPORT property set to
    TRUE.

After the change:
:   New accounts no longer have a `SNOWFLAKE` user automatically added to the account. The SNOWFLAKE\_SUPPORT property is set to FALSE for
    all users.

When this change becomes Generally Enabled, Snowflake will transfer ownership of all existing `SNOWFLAKE` users to the ACCOUNTADMIN
role. At that point, you can drop the `SNOWFLAKE` user from your account at your convenience.

Ref: 1976
