# SHOW/DESC SERVICE commands and SERVICES view (ACCOUNT\_USAGE and INFORMATION SCHEMA): New column MIN\_READY\_INSTANCES

Attention

This behavior change is in the 2024\_08 bundle.

For the current status of the bundle, refer to [Bundle History](/release-notes/bcr-bundles/2024_08_bundle#label-behavior-change-bundle-2024-08-status).

When this behavior change bundle is enabled, the output of the [SHOW SERVICES](/sql-reference/sql/show-services) and [DESCRIBE SERVICE](/sql-reference/sql/desc-service) commands, [SERVICES view (ACCOUNT\_USAGE)](/sql-reference/account-usage/services), and [SERVICES view (INFORMATION\_SCHEMA)](/sql-reference/info-schema/services) include the following new column:

| Column name | Description |
| --- | --- |
| MIN\_READY\_INSTANCES | Indicates the minimum service instances that must be ready for Snowflake to consider the service is ready to process requests. The default is the same value as the MIN\_INSTANCES. |

Expand

Show lessSee more

Ref: 1793
