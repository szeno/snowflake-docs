# GET\_LINEAGE function: New column in output

Attention

This behavior change is in the 2025\_05 bundle.

For the current status of the bundle, refer to [Bundle History](/release-notes/bcr-bundles/2025_05_bundle#label-behavior-change-bundle-2025-05-status).

When this behavior change bundle is enabled, the output of the
[SNOWFLAKE.CORE.GET\_LINEAGE](/sql-reference/functions/get_lineage-snowflake-core) function includes the following new column:

| Column name | Data type | Description |
| --- | --- | --- |
| `process` | VARIANT | Provides details about how lineage between the source object and target object was established. For example, it might include the query ID of a SQL query or the name of a stored procedure that moved data from the source object to the target object. |

Expand

Show lessSee more

Ref: 2059
