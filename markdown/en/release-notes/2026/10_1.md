# 10.1 Release Notes (with behavior changes): Jan 19, 2026-Jan 23, 2026

Attention

This release has completed. For differences between the in-advance and final versions of these release notes, see [Release notes change log](#label-rel-notes-change-log-10-1).

## Behavior change bundles

This release contains the following behavior change bundles:

| Bundle Name | Status in this Release | Previous Status |
| --- | --- | --- |
| [2026\_01](/release-notes/bcr-bundles/2026_01_bundle) | Disabled by default; admins can enable for testing | N/A (introduced in this release) |
| [2025\_07](/release-notes/bcr-bundles/2025_07_bundle) | Enabled by default; admins can disable for opt-out | Disabled by default |
| [2025\_06](/release-notes/bcr-bundles/2025_06_bundle) | Generally enabled; admins can no longer enable/disable | Enabled by default |

Expand

Show lessSee more

The status for each bundle will change again in the following behavior change release, planned for March 2-5, 2026; however, this
schedule is subject to change.

For more information about bundle statuses and how they may impact your accounts, see [About Behavior Changes](/release-notes/intro-bcr-releases).

## SQL updates

### Retrieve bind variable values (*General availability*)

The ability to retrieve bind variable values is now generally available and is no longer in Preview.

You can retrieve the values of the bind variables by using the BIND\_VALUES table function in the INFORMATION\_SCHEMA
schema. Using this function, you can retrieve bind variable values from any code that supports bind variables,
including JavaScript and Snowflake Scripting code.

Bind variable values for past queries are also visible in the `bind_values` column in the output for the
[QUERY\_HISTORY Account Usage view](/sql-reference/account-usage/query_history), the
[QUERY\_HISTORY Organization Usage view](/sql-reference/organization-usage/query_history), or the
[QUERY\_HISTORY function](/sql-reference/functions/query_history) in the INFORMATION\_SCHEMA schema.

For more information, see [Retrieve bind variable values](/sql-reference/bind-variables#label-bind-variables-retrieving-values).

## Release notes change log

| Announcement | Update | Date |
| --- | --- | --- |
| Release notes | Initial publication (preview) | Jan 16, 2026 |

Expand

Show lessSee more
