# August 26-30, 2024 — 8.32 Release Notes (with behavior changes)

Attention

The release has completed.

For differences between the in-advance and final versions of these release notes, see [Release notes change log](#label-rel-notes-change-log-8-32).

## Behavior change bundles

This release contains the following behavior change bundles:

| Bundle Name | Status in this Release | Previous Status |
| --- | --- | --- |
| [2024\_07](/release-notes/bcr-bundles/2024_07_bundle) | Disabled by default; admins can enable for testing | N/A (introduced in this release) |
| [2024\_06](/release-notes/bcr-bundles/2024_06_bundle) | Enabled by default; admins can disable for opt-out | Disabled by default |
| [2024\_05](/release-notes/bcr-bundles/2024_05_bundle) | Generally enabled; admins can no longer enable/disable | Enabled by default |

Expand

Show lessSee more

The status for each bundle will change in the next behavior change release, planned for September 2024; however, this schedule
is subject to change.

For more information about bundle statuses and how they may impact your accounts, see [About Behavior Changes](/release-notes/intro-bcr-releases).

## SQL updates

### New SQL functions

The following function(s) are now available with this release:

| Function category | New function | Description |
| --- | --- | --- |
| String & binary | SEARCH\_IP (Preview) | Searches for valid IPv4 addresses in specified character-string columns from one or more tables, including fields in VARIANT, OBJECT, and ARRAY columns. |

Expand

Show lessSee more

## Data pipeline updates

### Tasks: A new option for ALTER TASK

With this release, the ALTER TASK command supports a new option, `REMOVE WHEN`. You can use this option to easily remove the task’s condition. The syntax will be as follows:

Copy code

```
ALTER TASK [ IF EXISTS ] <name> REMOVE WHEN
```

For more information, see [ALTER TASK](/sql-reference/sql/alter-task).

## Release notes change log

| Announcement | Update | Date |
| --- | --- | --- |
| Release notes | Initial publication (preview) | 23-Aug-24 |

Expand

Show lessSee more
