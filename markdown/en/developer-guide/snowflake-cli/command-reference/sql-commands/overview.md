# snow sql commands

`snow sql` is the only SQL command in Snowflake CLI. Pass a query with `-q`, a file with `-f`, or pipe SQL on stdin.

See [Executing SQL statements](/developer-guide/snowflake-cli/sql/execute-sql) and the [snow sql](/developer-guide/snowflake-cli/command-reference/sql-commands/sql) command reference.
